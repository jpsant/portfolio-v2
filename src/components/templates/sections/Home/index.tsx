import styles from './styles.module.scss';

const HomeSection = () => {
  return (
    <section className={styles.homeSection}>
      <h1>HomeSection!</h1>
    </section>
  );
};

export default HomeSection;
