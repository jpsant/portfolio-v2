import { Container } from '@material-ui/core';
import './styles.scss';

const SkillsTemplate = () => {
  return (
    <section className="skillsTemplate">
      <Container maxWidth="lg">
        <h1>Skills template</h1>
      </Container>
    </section>
  );
};

export default SkillsTemplate;
